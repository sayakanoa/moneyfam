<div class="container py-5">
    <div class="row">
        <%= link_to "カレンダー", events_path(user_id: current_user.id)%>
    </div>

    <div class="row">
        <%= link_to "支出一覧", payments_path(user_id: current_user.id)%>
    </div>

    <div class="row">
        <%= link_to "購入検討リスト", wants_path(user_id: current_user.id)%>
    </div>

    <div class="row">
        <%= link_to "購入リスト", buys_path(user_id: current_user.id)%>
    </div>


</div>

