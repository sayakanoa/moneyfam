<h1>カレンダー画面</h1>

<%= month_calendar events: @events do |date, events| %>
    <%= date.day %>
<% end %>


<% @events.each do |event| %>
    <div>
      <%= event.title %>
    </div>
<% end %>


<%= link_to 'イベント追加', new_event_path %>